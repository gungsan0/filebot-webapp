{
  "name": "filebot-webapp",
  "version": "1.3.1",
  "description": "FileBot-style media file renamer with a web UI — scan a folder, match metadata, apply Plex-friendly format presets, preview, and rename.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build:pkg": "bash scripts/build-pkg.sh"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "license": "MIT"
}
